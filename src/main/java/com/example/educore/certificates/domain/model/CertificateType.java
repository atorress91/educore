package com.example.educore.certificates.domain.model;

/** The kinds of certificate the institution issues. */
public enum CertificateType {
    /** Grade certification ("Certificación de notas"). */
    GRADES,
    /** Enrollment certificate ("Constancia de matrícula"). */
    ENROLLMENT
}
